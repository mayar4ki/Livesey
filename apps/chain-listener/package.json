{
  "name": "chain-listener",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e .env -- tsx src/chain-listener.ts",
    "build": "tsc",
    "start": "node dist/chain-listener.js",
    "typecheck": "tsc --noEmit",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "dependencies": {
    "viem": "catalog:",
    "zod": "catalog:",
    "@acme/db": "workspace:*",
    "@acme/queue": "workspace:*",
    "@acme/smart-contract": "workspace:*"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "dotenv-cli": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
