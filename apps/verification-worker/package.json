{
  "name": "verification-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e .env -- tsx src/verification-worker.ts",
    "build": "tsc",
    "start": "node dist/verification-worker.js",
    "typecheck": "tsc --noEmit",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "dependencies": {
    "viem": "catalog:",
    "@acme/shared": "workspace:*",
    "@acme/db": "workspace:*",
    "@acme/queue": "workspace:*",
    "@acme/smart-contract": "workspace:*"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "dotenv-cli": "catalog:",
    "tsx": "^4.20.6",
    "typescript": "catalog:"
  }
}
