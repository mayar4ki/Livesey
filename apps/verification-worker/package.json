{
  "name": "verification-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm with-env tsx src/verification-worker.ts",
    "build": "tsc",
    "with-env": "dotenv -e ../../.env --",
    "start": "node dist/verification-worker.js",
    "typecheck": "tsc --noEmit",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "dependencies": {
    "viem": "catalog:",
    "@acme/db": "workspace:*",
    "@acme/queue": "workspace:*",
    "@acme/smart-contract": "workspace:*",
    "bullmq": "^5.23.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "dotenv-cli": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
