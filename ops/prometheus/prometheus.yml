global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: back-end
    metrics_path: /api/metrics
    static_configs:
      - targets:
          - back-end:3002

  - job_name: cadvisor
    static_configs:
      - targets:
          - cadvisor:8080
